export type AgainstTypeStrength = 'NO_EFFECT' | 'NOT_VERY_EFFECTIVE' | 'NORMAL' | 'SUPER_EFFECTIVE';

export enum RelationLabel {
  AGAINST_TYPE = 'AGAINST_TYPE'
}

export enum NodeLabel {
  TYPE = 'Type'
}
